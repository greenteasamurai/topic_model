[phases.setup]
nixPkgs = ["nodejs_latest"]

[phases.install]
cmds = ["cd frontend && npm ci && npm run build", "pip install -r requirements.txt", "python -m spacy download en_core_web_sm"]

[start]
cmd = "cd backend && uvicorn api:app --host 0.0.0.0 --port $PORT"
